algorithm TestStructure
structure Person
    name: string;
    age: integer;
    salary: real;
endstructure

var
    p: Person;

begin
    p.name := "Alice";
    p.age := 30;
    p.salary := 4500.5;

    write("Employee:");
    write(p.name);
    write(p.age);
    write(p.salary);
end.
