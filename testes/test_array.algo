algorithm TestArray
var
    a: array[4] of integer;
    i, sum: integer;
begin
    a[0] := 2;
    a[1] := 4;
    a[2] := 6;
    a[3] := 8;

    sum := 0;
    for i from 0 to 3 do
        sum := sum + a[i];
    endfor

    write("array sum:");
    write(sum);
end.
