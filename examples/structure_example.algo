algorithm StructureDemo
structure Person
    name : string;
    age: integer;
endstructure

var
    person: Person;
begin
    person.name := Melissa;
    person.age := 19;
    write("Name :");
    write(person.name);
    write("Age:");
    write(person.age);
end.
